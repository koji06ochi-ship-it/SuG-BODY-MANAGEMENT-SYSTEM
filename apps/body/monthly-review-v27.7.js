(()=>{'use strict';const LOG='sug_body_training_log_v1',MONTH='sug_body_monthly_review_v1';const $=s=>document.querySelector(s),j=(k,f)=>{try{return JSON.parse(localStorage.getItem(k)||'null')??f}catch{return f}};const monthKey=()=>{const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`};const AREA={push:'胸・肩・三頭',pull:'背中・二頭',legs:'脚・臀部',shoulder:'肩',care:'CARE',recovery:'回復'};function logsInMonth(key){return j(LOG,[]).filter(x=>String(x.date||'').startsWith(key))}function scoreArea(logs,area){const xs=logs.filter(x=>x.area===area),sets=xs.reduce((a,l)=>a+(l.items||[]).reduce((s,i)=>s+(+i.doneSets||+i.sets||0),0),e=xs.flatMap(l=>l.items||[]).map(i=>+i.e1rm||0).filter(Boolean);let trend=0;if(e.length>=2)trend=(e[0]-e[e.length-1])/Math.max(1,e[e.length-1]);return{area,sessions:xs.length,sets,e1rmTrend:trend}}function priority(logs){const areas=['push','pull','legs','shoulder'].map(a=>scoreArea(logs,a));const ranked=[...areas].sort((a,b)=>{const sa=(a.sessions<3?2:0)+(a.sets<12?1:0)+(a.e1rmTrend<0?1:0),sb=(b.sessions<3?2:0)+(b.sets<12?1:0)+(b.e1rmTrend<0?1:0);return sb-sa||a.sets-b.sets});return{top:ranked.slice(0,3),maintain:ranked.slice(3)}}function render(){let card=$('#monthlyReviewCard');if(!card){const hist=$('#history')?.closest('.card');if(!hist)return;card=document.createElement('section');card.id='monthlyReviewCard';card.className='card';hist.after(card)}const key=monthKey(),logs=logsInMonth(key),p=priority(logs),rec=logs.map(x=>+x.recovery||0).filter(Boolean),avgRec=rec.length?(rec.reduce((a,b)=>a+b,0)/rec.length).toFixed(1):'--';card.innerHTML=`<div class="ey">MONTHLY REVIEW</div><h2>${key} 月次比較</h2><div class="sub">写真比較の土台＋体重/e1RM/週SET/回復をまとめて来月方針を決める。</div><div style="display:grid;grid-template-columns:repeat(3,1fr);gap:7px;margin-top:12px"><div class="metric"><span>SESSIONS</span><b>${logs.length}</b></div><div class="metric"><span>RECOVERY</span><b>${avgRec}</b></div><div class="metric"><span>PHOTO</span><b>準備中</b></div></div><div style="margin-top:14px"><div class="ey">PRIORITY TOP3</div>${p.top.map((x,i)=>`<div class="historyrow"><b>${i+1}. ${AREA[x.area]}</b><span>${x.sessions}回｜${x.sets}SET${x.e1rmTrend?`｜e1RM ${(x.e1rmTrend*100).toFixed(1)}%`:''}</span></div>`).join('')||'<div class="empty">月内データ不足</div>'}</div><div style="margin-top:10px"><div class="ey">MAINTAIN</div>${p.maintain.map(x=>`<div class="historyrow"><b>${AREA[x.area]}</b><span>${x.sessions}回｜${x.sets}SET</span></div>`).join('')||'<div class="empty">判定待ち</div>'}</div><div style="margin-top:10px"><div class="ey">NEXT MONTH</div><div class="sub">${p.top.length?`優先：${p.top.map(x=>AREA[x.area]).join(' → ')}。週SET反応とNEXT LOADを優先部位へ寄せる。`:'トレーニング記録が増えると自動で優先部位を決定します。'}</div></div>`;localStorage.setItem(MONTH,JSON.stringify({month:key,updatedAt:new Date().toISOString(),priority:p,avgRecovery:avgRec}))}new MutationObserver(()=>render()).observe(document.documentElement,{subtree:true,childList:true});setTimeout(render,120);window.addEventListener('storage',render);window.SuGMonthlyReviewV277={render,priority};})();