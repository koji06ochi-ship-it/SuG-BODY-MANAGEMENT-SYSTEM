(()=>{'use strict';const $=id=>document.getElementById(id);const KEY='sug_body_daily_v1';const TODAY=()=>{const d=new Date(),p=n=>String(n).padStart(2,'0');return `${d.getFullYear()}-${p(d.getMonth()+1)}-${p(d.getDate())}`};let s={appetite:null,available:60,precheck:null,level:2,area:'auto',sleep:null,fatigue:null,stress:null,pain:null};try{const old=JSON.parse(localStorage.getItem(KEY)||'null');if(old?.date===TODAY())s={...s,...old}}catch{}const labels={appetite:['かなり低い','低い','普通','良い','かなり良い'],precheck:['休養寄り','軽め','普通','いける','かなり良い'],sleep:['かなり悪い','悪い','普通','良い','かなり良い'],fatigue:['ほぼなし','軽い','普通','強い','かなり強い'],stress:['ほぼなし','軽い','普通','強い','かなり強い'],pain:['なし','軽い','気になる','強い','かなり強い']};const IDEAL={slim:{name:'SLIM',volume:-1,rir:1,note:'引き締め優先'},athletic:{name:'ATHLETIC',volume:0,rir:0,note:'バランス優先'},fitness:{name:'FITNESS',volume:1,rir:0,note:'筋量と輪郭を両立'},muscular:{name:'MUSCULAR',volume:1,rir:0,note:'筋量優先'},physique:{name:'PHYSIQUE',volume:1,rir:-1,note:'肩・背中・脚の見映え優先'}};function scale(id,key){const el=$(id);el.innerHTML=[1,2,3,4,5].map(v=>`<button data-v="${v}" class="${s[key]===v?'on':''}">${v}<small>${labels[key][v-1]}</small></button>`).join('');el.querySelectorAll('button').forEach(b=>b.onclick=()=>{s[key]=+b.dataset.v;render();save()})}function save(){localStorage.setItem(KEY,JSON.stringify({...s,date:TODAY()}))}function autoArea(){const d=new Date().getDay();return ({1:'push',2:'pull',3:'legs',4:'push',5:'pull'})[d]||'recovery'}function ideal(){try{return JSON.parse(localStorage.getItem('sug_v27_ideal')||localStorage.getItem('sug_ideal_shadow_v2')||'null')}catch{return null}}function idealType(){const v=ideal();return v?.idealVisionType||v?.type||null}function clampRir(text,delta){const nums=String(text).match(/\d+/g)?.map(Number)||[2];const a=Math.max(1,Math.min(4,nums[0]+delta)),b=Math.max(a,Math.min(4,(nums[1]??nums[0])+delta));return a===b?String(a):`${a}–${b}`}function decide(){if(!s.appetite||!s.precheck||!s.sleep||!s.fatigue||!s.stress||!s.pain){$('decision').textContent='INPUT';$('decisionSub').textContent='今日の条件を入力';$('prescription').innerHTML='';$('menu').innerHTML='<div class="empty">入力がそろうと自動生成します。</div>';return}const rec=(s.appetite+s.precheck+s.sleep+(6-s.fatigue)+(6-s.stress)+(6-s.pain))/6;let mode='TRAIN',rir='2',intensity='NORMAL',time=Math.min(s.available,60),reason='通常トレーニング';if(s.pain>=4||s.area==='care'){mode='CARE FIRST';rir='3–4';intensity='LOW';time=Math.min(s.available,30);reason='痛みを優先'}else if(rec<2.5){mode='RECOVERY';rir='3–4';intensity='LOW';time=Math.min(s.available,30);reason='全身回復を優先'}else if(rec<3.5){mode='HOLD';rir='2–3';intensity='MID';time=Math.min(s.available,45);reason='ボリューム維持'}else if(s.level===1){rir='3';intensity='MID';reason='フォームと再現性を優先'}else if(s.level===3){rir='1–2';intensity='NORMAL+';reason='高い操作精度を前提に負荷'}const chosen=s.area==='auto'?autoArea():s.area;const it=idealType(),ip=IDEAL[it];if(mode==='TRAIN'&&ip){rir=clampRir(rir,ip.rir);if(it==='physique'&&s.level>=2)intensity='NORMAL+'}const idealLabel=ip?`${ip.name}｜${ip.note}`:'IDEAL未設定';$('decision').textContent=mode;$('decisionSub').textContent=`${reason}｜回復 ${rec.toFixed(1)}/5｜${idealLabel}`;$('prescription').innerHTML=`<div><span>TIME</span><b>${time}分</b></div><div><span>RIR</span><b>${rir}</b></div><div><span>INTENSITY</span><b>${intensity}</b></div><div><span>AREA</span><b>${chosen.toUpperCase()}</b></div>`;buildMenu(mode,chosen,time,it)}const base={push:[['DBインクラインプレス',4,8],['DBショルダープレス',3,8],['DBサイドレイズ',4,15],['三頭プレスダウン',3,12]],pull:[['DBベントオーバーロー',4,10],['バーベル広背筋ロー',4,8],['ラットプルダウン',3,10],['腹筋',3,15]],legs:[['スクワット',3,6],['RDL',4,8],['ブルガリアンスクワット',3,8],['ケーブルアブダクション',3,15],['ケーブルアダクション',3,15]],shoulder:[['DBサイドレイズ',4,15],['リアデルト',3,15],['ショルダープレス',3,8],['腹筋',3,15]],recovery:[['ウォーキング',1,20],['モビリティ',2,10]],care:[['CARE',1,10],['ウォーキング',1,15],['再評価',1,1]]};function buildMenu(mode,area,time,it){let m=mode==='CARE FIRST'?base.care:mode==='RECOVERY'?base.recovery:(base[area]||base.push);let cap=time<=30?3:time<=45?4:99;if(s.level===1&&mode==='TRAIN')cap=Math.min(cap,3);m=m.slice(0,cap).map((x,i)=>{let sets=x[1],rep=x[2];if(time<=30&&sets>3)sets=3;if(mode==='HOLD'&&sets>3)sets=3;if(mode==='TRAIN'&&s.level===1){sets=Math.min(sets,3);if(rep<8)rep=8}if(mode==='TRAIN'&&s.level===3&&time>=45&&i===0)sets+=1;const ip=IDEAL[it];if(mode==='TRAIN'&&ip){if(ip.volume<0&&i>0)sets=Math.max(2,sets-1);if(ip.volume>0&&time>=45){const physiqueBias=it==='physique'&&(['push','pull','shoulder','legs'].includes(area));if((it==='muscular'&&i<3)||(it==='fitness'&&i>=2)||(physiqueBias&&i>=2))sets+=1}}return [x[0],sets,rep]});$('menu').innerHTML=m.map(([n,set,rep],i)=>`<div class="exercise"><div><span>${String(i+1).padStart(2,'0')}</span><b>${n}</b></div><strong>${set} × ${rep}</strong></div>`).join('')}function render(){scale('appetite','appetite');scale('precheck','precheck');scale('sleep','sleep');scale('fatigue','fatigue');scale('stress','stress');scale('pain','pain');$('available').value=s.available;$('level').value=s.level;$('area').value=s.area;decide()}['available','level','area'].forEach(id=>$(id).onchange=e=>{s[id]=id==='area'?e.target.value:+e.target.value;save();render()});window.addEventListener('sug:ideal-change',render);window.SuGBody={receiveNative:p=>{if(!p)return;const v=(...a)=>a.find(x=>x!=null);const st=v(p.steps,p.stepCount),sl=v(p.sleepHours,p.sleep),hr=v(p.heartRate,p.latestHeartRate,p.restingHeartRate),wt=v(p.weightKg,p.weight);if(st!=null)$('steps').textContent=Number(st).toLocaleString();if(sl!=null)$('sleepHealth').textContent=`${Number(sl).toFixed(1)}h`;if(hr!=null)$('heart').textContent=`${Math.round(Number(hr))}`;if(wt!=null)$('weight').textContent=`${Number(wt).toFixed(1)}kg`}};window.SuGV27=window.SuGBody;render();window.SuGBody.receiveNative(window.__SUG_NATIVE_HEALTH__);})();