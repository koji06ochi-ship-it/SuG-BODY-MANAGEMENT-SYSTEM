(()=>{'use strict';const V='26.5.184',K='sug_training_sessions_v1';function latest(){try{const a=JSON.parse(localStorage.getItem(K)||'[]');return Array.isArray(a)&&a.length?{arr:a,rec:a[a.length-1]}:null}catch(_e){return null}}function localDate(iso){const d=new Date(iso||Date.now());const y=d.getFullYear(),m=String(d.getMonth()+1).padStart(2,'0'),day=String(d.getDate()).padStart(2,'0');return `${y}-${m}-${day}`}function sync(){const pack=latest();if(!pack||!pack.rec||pack.rec.mainSyncedAt)return false;const rec=pack.rec;let mem=null;try{if(typeof window.m==='function')mem=window.m()}catch(_e){}if(!mem)return false;mem.training=Array.isArray(mem.training)?mem.training:[];const sid=String(rec.at||Date.now()),date=localDate(rec.at);let added=0;for(const ex of (rec.exercises||[])){const done=(ex.sets||[]).filter(s=>s&&s.reps!=null);if(!done.length)continue;if(mem.training.some(r=>r&&r.sessionFlowId===sid&&r.exercise===ex.name))continue;const first=done[0];const details=done.map((s,i)=>({set:Number(s.set||i+1),weight:s.load==null?0:Number(s.load),reps:Number(s.reps||0),rir:s.rir==null?0:Number(s.rir),rest:null}));mem.training.push({date,exercise:String(ex.name||''),part:String(ex.part||'その他'),pof:String(ex.pof||'その他'),setMethod:'straight',setGroup:'',secondaryParts:[],indirectFactor:0.5,weight:first.load==null?0:Number(first.load),reps:Number(first.reps||0),sets:details.length,rir:first.rir==null?0:Number(first.rir),memo:String(rec.memo||''),setDetails:details,plannedRestSec:null,restDataVersion:1,source:'today-flow',sessionFlowId:sid,sessionCompletion:String(rec.completion||''),sessionRpe:rec.rpe??null,sessionPain:rec.pain??0});added++}if(!added)return false;mem.training.sort((a,b)=>String(a.date||'').localeCompare(String(b.date||'')));rec.mainSyncedAt=new Date().toISOString();pack.arr[pack.arr.length-1]=rec;localStorage.setItem(K,JSON.stringify(pack.arr.slice(-100)));try{if(typeof window.persist==='function')window.persist()}catch(_e){}setTimeout(()=>{try{if(typeof window.renderSmartCore==='function')window.renderSmartCore(false)}catch(_e){}},80);return true}document.addEventListener('click',e=>{const b=e.target&&e.target.closest&&e.target.closest('#s176Done');if(!b)return;sync()},true);window.SUG_SYNC_TODAY_FLOW_TO_TRAINING=sync;window.__SUG_SESSION_TRAINING_BRIDGE_VERSION__=V})();