(()=>{'use strict';const V='26.5.197',K='sug_training_sessions_v1';const member=new URLSearchParams(location.search).get('entry')==='member';if(!member)return;function dayKey(v=Date.now()){const d=new Date(v);return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}function closedToday(){try{const a=JSON.parse(localStorage.getItem(K)||'[]');return Array.isArray(a)&&a.some(r=>r?.flowClosedAt&&dayKey(r.at)===dayKey())}catch(_e){return false}}function fallback(){if(closedToday()||window.__SUG_FLOW_CONTROLLER_VERSION__)return;const app=document.getElementById('smartAppetite'),launch=document.getElementById('sugIdealLaunch'),modal=document.getElementById('idealVisionModal');if(app||launch||modal)return;if(document.getElementById('sug-guided-flow'))return;const box=document.createElement('section');box.id='sug-guided-flow';box.innerHTML='<div style="color:#d8b34b;font-size:12px;font-weight:800">TODAY FLOW</div><h2>理想の身体を選ぶ</h2><p>初回だけ設定。選んだら自動で次へ進みます。</p><button id="sugIdealLaunch" type="button">理想を選ぶ →</button>';(document.querySelector('header')||document.body).insertAdjacentElement('afterend',box)}function boot(){document.body.classList.add('sug-today-flow');setTimeout(fallback,1600);window.addEventListener('pageshow',()=>setTimeout(fallback,700));window.__SUG_FLOW_BOOTSTRAP_VERSION__=V}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot()})();